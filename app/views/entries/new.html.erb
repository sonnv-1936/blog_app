<%= form_for @entry do |f| %>
  <%= render "shared/error_messages", resource: @entry %>

  <%= f.text_field :title, placeholder: "Entry's title..." %><br>
  <%= f.text_area :body, placeholder: "Entry's body..." %>
  <%= f.submit "Post" %>
<% end %>
