<b><%= link_to comment.user.name, comment.user %></b>
<i>
  commented on <b><%= comment.created_at.strftime "%m/%d/%y" %></b> at
  <b><%= comment.created_at.strftime "%H:%M" %></b>

  <% if current_user&.admin? %>
    <b><%= link_to "Delete", comment, method: :delete,
      data: {confirm: "Are you sure?"} %></b>
  <% end %>
</i><br>

<%= comment.content %><br>
